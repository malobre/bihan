---
"@malobre/bihan": minor
---

`withAuthorization`: add challenge parameter

`challenge` is the value of the `WWW-Authenticate` header returned with the
response when the `Authorization` header isn't set.
